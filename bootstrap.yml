---
- hosts: windows
  gather_facts: no
  connection: local
  tasks:
    - name: run a cmd.exe command
      psexec:
        hostname: "{{ansible_host}}"
        encrypt: no
        process_username: System
        executable: cmd.exe
        arguments: /c echo Hello World